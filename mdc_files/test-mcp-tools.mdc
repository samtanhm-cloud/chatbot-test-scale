# Playwright MCP Tool Test

Testing with the ACTUAL tool names (all lowercase).

## Test 1: Navigate to example.com
```mcp
{
  "tool": "playwright_navigate",
  "params": {
    "url": "https://example.com"
  }
}
```

## Test 2: Get page text
```mcp
{
  "tool": "playwright_get_visible_text",
  "params": {}
}
```

## Test 3: Take screenshot
```mcp
{
  "tool": "playwright_screenshot",
  "params": {
    "name": "example-screenshot",
    "width": 1280,
    "height": 720
  }
}
```

## Test 4: Close browser
```mcp
{
  "tool": "playwright_close",
  "params": {}
}
```
