# npm configuration for Streamlit Cloud deployment

# Ensure dependencies are installed
save-exact=false
package-lock=true

# Avoid unnecessary optional dependencies
optional=false

# Use registry defaults
registry=https://registry.npmjs.org/
